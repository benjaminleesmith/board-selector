apply from: "$projectDir/../server.gradle"

dependencies {
    compile project(":components:board-review")
    testCompile project(":components:test-support")
    testCompile('org.springframework.boot:spring-boot-starter-test')
}
